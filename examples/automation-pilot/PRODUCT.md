# Product

A local operator converts a roster CSV into a normalized JSON report. Valid rows are sorted and assigned a training priority; malformed rows become explicit errors without discarding good work. The operator can preview exactly what would be written and safely repeat the operation.
