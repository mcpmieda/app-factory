# Product

Focus Lens reduces a repetitive scan on a controlled task page. One injected button highlights only elements explicitly marked as actionable; a second click removes every change. The observable contract is useful, predictable and reversible without retaining page content.
