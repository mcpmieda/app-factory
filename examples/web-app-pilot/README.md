# Web app pilot — Encontro

Small complete end-user booking journey used to validate the App Factory `web-app` route.

```sh
npm ci
npm run format:check
npm run lint
npm run typecheck
npm test
npm run build
npm audit --audit-level=high
npx playwright install chromium
npm run test:e2e
```

Read `PROJECT_STATE.md` before continuing.
