# Website pilot — Escola Horizonte

Small complete institutional website used to validate the App Factory `website` route.

## Commands

```sh
npm ci
npm run format:check
npm run typecheck
npm run build
npm audit --audit-level=high
npx playwright install chromium
npm run test:e2e
```

See `PROJECT_STATE.md` before continuing work.
